turn : pred.
rock : pred.
paper : pred.
scissors : pred.
win : pred.
lose : pred.
token : pred.

turn player : pred.
rock player : pred.
paper player : pred.
scissors player : pred.
win player : pred.
lose player : pred.
token player : pred.

stage game = {
pick_rock
	: turn A -o rock A * token A.
pick_paper
	: turn A -o paper A * token A.
pick_scissor
	: turn A -o scissor A * token A.
}
#interactive game.
